#include <stdio.h>
/**
 * main is the entry point
 *This is a c code that prints a char using puts function
 *the return of the value 0 means that the execution has succeded
*/
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
